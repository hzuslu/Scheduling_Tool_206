To get started, first install the necessary dependencies by running:

npm install

Once the dependencies are installed, you can launch the project by executing:

ng serve

Additionally, for a more enjoyable viewing experience, you might want to consider enabling dark mode. It not only enhances the visual aesthetics but also helps reduce eye strain, especially in dimly lit environments.